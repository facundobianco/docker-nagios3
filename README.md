# Docker container for Nagios3

## Plus

* Uses Debian 8.2
* Uses exim4 as MTA

## What you should configure

* **exim4:** files `conf/update-exim4.conf.conf` and `conf/passwd.client`
* **Nagios user:** by default, *nagiosadmin:admin*
